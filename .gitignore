node_modules/
dist/
.tmp/
.browser/
screenshots/
*.local
*.tsbuildinfo

# Staged OCR runtime — regenerated by `npm run setup:ocr`
public/ocr/

# Never commit patient data or encrypted session archives
*.enc

# Scratch files from diagnostic sessions
.tmp-*
.tmpprofile*

# Secrets — never commit. The OpenAI key belongs in Vercel's environment
# variables, or in a local .env that stays on this machine.
.env
.env.*
!.env.example
*.pem
*.key
